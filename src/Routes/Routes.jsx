import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
    {
        path: '/',
        element: <h1 className="text-4xl font-black">This is client side</h1>
    }
])

export default router;